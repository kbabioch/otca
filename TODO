- Register trap for error handling of openssl command block

- Option for tarball?

- Option to keep keys/files

- Fix years handling (should be relative to start date, not relativ to now)

- Verbose flag should also impact OpenSSL output

- Debug flag (keep files and directories around)

- Parameter for OpenSSL extension name

